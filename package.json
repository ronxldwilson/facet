{
  "name": "facet",
  "version": "0.1.0",
  "description": "One site, every aspect at once — multi-viewport rendering engine with mirrored interactions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
